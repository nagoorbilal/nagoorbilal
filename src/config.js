var currentUrl=window.location.href;
var getHttp = currentUrl.split('/'); 

export const config = {
                    Api: getHttp[0]+'//redaarc.com/api/armfishtest/v1/',
                    
                }

